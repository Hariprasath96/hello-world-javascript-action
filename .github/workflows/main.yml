on: [push]

jobs:
  product-list:
    runs-on: ubuntu-latest
    name: Fetch all available products
    outputs:
      products: ${{steps.list-products.outputs.products}}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: List all products
        id: list-products
        uses: ./

      - name: Get the output products-list
        run: echo "All products - ${{ steps.list-products.outputs.products }}"

  check:
    needs: [product-list]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        manifest: ${{ fromJson(needs.product-list.outputs.products) }}
    steps:
      - name: test
        id: sample
        run: |
          echo "${{ toJson(matrix.manifest) }}"
          echo "::set-output name=productName::${{ toJson(matrix.manifest.name) }}"
          echo "test_p=${{ toJson(matrix.manifest.name) }}" >> $GITHUB_ENV

      - uses: actions/checkout@v2 

      - name: trigger
        uses: ./products-1/${{ env.test_p }}
        with:
          version_to_deploy: latest
        # run: |
        #   echo "product name--" ${{steps.sample.outputs.productName}}

  # TriggerProductWorkFlow:
  #   needs: [product-list]
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       manifest: ${{ fromJson(needs.product-list.outputs.products) }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v2

  #     - uses: ./opschain-esg.yml
  #       with:
  #         version_to_deploy: latest
  #         environment-to-deploy: stage
